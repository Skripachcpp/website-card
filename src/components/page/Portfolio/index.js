import React, {Component} from 'react';
import cx from 'classnames';
import styles from './styles.module.sass';

class Portfolio extends Component {
  render() {
    return (
      <div className={cx('Portfolio', styles.component)}>
      </div>
    );
  }
}

export default Portfolio;